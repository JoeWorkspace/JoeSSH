# JoeSSH Light UI Masterboard — ImageGen Prompt

Mode: built-in ImageGen

Use case: style-transfer

Asset type: JoeSSH cross-platform high-fidelity Light theme UI masterboard

Input images: Image 1 is the edit target and existing approved Dark-theme
masterboard.

Primary request: Create the parallel Light-theme version of Image 1. Change
only the color theme, surface treatment, borders, shadows, and light-mode
contrast. Preserve the exact 16:9 composition, three interface frames,
proportions, component geometry, information hierarchy, feature coverage, and
straight-on framing.

Light visual system: cool off-white presentation canvas `#eef3f4`; app
backgrounds `#f3f6f7`; raised cards and panels `#ffffff`; soft secondary
surfaces `#f7faf9`; primary text `#102026`; muted text `#50636c`; one-pixel
borders `#d8e2e5`; emerald primary `#087e66` with stronger `#06624f` and soft
mint `#e5f4f0`; restrained blue, amber, red, violet and green status colors;
subtle neutral shadows and no glassy haze.

Desktop requirement: convert navigation, sidebars, toolbar, tabs, context
inspector and cards to the Light theme, but keep the two terminal content
canvases deep graphite `#091016` with pale monospace text for sustained terminal
readability.

Web Admin requirement: keep the data-heavy workspace fully Light, make
navigation a refined cool-white rail with an emerald active state, preserve
table density and card hierarchy.

Mobile requirement: convert the phone shell, hero, cards, metrics, status,
device, preview and emergency panels to Light surfaces; keep only the terminal
preview panel deep graphite.

Text (verbatim, preserve these exact top-level labels): "JoeSSH", "工作台",
"团队运营", "同步与应急访问".

Constraints: change only theme styling; keep all layouts and features
unchanged; strong accessibility contrast; clear selected, active, warning and
offline states; crisp premium production UI, not concept art; no perspective
change; no new components; no removed components; no people; no decorative
illustrations; no watermark; no unrelated logos; no random extra text.

Avoid: beige warmth, washed-out gray, low-contrast borders, blue as the primary
brand color, all-white terminal panes, neon, excessive gradients,
glassmorphism, gaming HUD styling, and illegible microtext.
