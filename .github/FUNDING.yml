# Intentionally empty: JoeSSH has no fully verified GitHub Funding destination
# yet. A repository voluntary-support page may be visible, but it must not be
# promoted through GitHub's Sponsor button until every live verification below
# passes.
#
# This comments-only file is fail-closed. GitHub must not render a sponsor
# button that points to a placeholder, another person's account, or an
# unapproved checkout.
#
# After ownership, logged-out page, recipient-display, small-payment,
# payment-limitations/non-purchase wording, and payout verification, follow
# docs/funding-and-checkout.md. Add exactly one verified destination and update
# funding-operator-attestation.json in the same reviewed commit. Do not commit
# template handles or URLs as active YAML values.
